---
layout: ../layouts/Sampler.astro
title: Component sampler
---
import Tip from '../components/textbook/Tip.astro';
import Remember from '../components/textbook/Remember.astro';
import Warning from '../components/textbook/Warning.astro';
import GoingDeeper from '../components/textbook/GoingDeeper.astro';
import InXRayCalc from '../components/textbook/InXRayCalc.astro';
import Figure from '../components/textbook/Figure.astro';
import DataTable from '../components/textbook/DataTable.astro';
import PeriodFromOrders from '../components/widgets/PeriodFromOrders.svelte';
import SensitivityCheck from '../components/widgets/SensitivityCheck.svelte';

_Not part of the book. Every building block a chapter may use, rendered once, so that the web build and the print pipeline can be checked against it. Placeholder text only. Nothing here is a claim of the book._

## Icons

<Tip>Placeholder text for a Tip.</Tip>

<Remember>Placeholder text for a Remember.</Remember>

<Warning title="with a title">Placeholder text for a Warning.</Warning>

<InXRayCalc>**Data > Normalize (Auto)**</InXRayCalc>

<GoingDeeper title="the Bragg law with refraction">

$$
\sin^2\theta_m = \left(\frac{m\lambda}{2D}\right)^2 + 2\dbar
$$

</GoingDeeper>

## Math and units

Inline: a length of $35\,\Ang$, a floor of $1\times10^{-7}$, and an angle of 2θ = 1.2° on the chart.

## Figure and table

<Figure id="fig-sampler-1" number="0-1" caption="A caption with *emphasis*, a subscript θ_c and a superscript Å^-1.">
  <svg viewBox="0 0 200 40" role="img" aria-label="placeholder"><rect x="2" y="2" width="196" height="36" fill="none" stroke="currentColor" /></svg>
</Figure>

<DataTable keepHeaderCase headers={['Symbol', 'Unit']} rows={[['σ', 'Å'], ['ρ', 'g/cm^3']]} />

A link to the figure: [Figure 0-1](#fig-sampler-1).

## Widget

<PeriodFromOrders client:visible />

<SensitivityCheck client:visible />

## One source, two outputs

:::print-only
PRINT-ONLY: this paragraph appears in the PDF and never on screen.
:::

:::web-only
WEB-ONLY: this paragraph appears on screen and never in the PDF.
:::

A ratio of 1:3 in prose stays literal text.
